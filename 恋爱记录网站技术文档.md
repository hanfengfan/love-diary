# 恋爱记录网站技术文档

## 项目概述

恋爱记录网站是一个用于记录、管理和展示恋爱期间美好回忆的个人应用，支持上传和管理照片、视频、文字等多种形式的内容。网站采用单管理员模式，只有管理员可以登录进行内容管理，其他访客只能浏览公开的内容。

## 技术栈

### 前端
- **框架**: Vue.js 3
- **构建工具**: Vite
- **UI组件库**: Element Plus 或 Ant Design Vue
- **状态管理**: Pinia
- **路由**: Vue Router
- **HTTP客户端**: Axios
- **样式**: CSS3 + SASS/SCSS

### 后端
- **框架**: Flask
- **数据库**: SQLite
- **ORM**: SQLAlchemy
- **API文档**: Flask-RESTX (Swagger)
- **文件上传**: Flask-WTF + Werkzeug
- **身份验证**: Flask-Login + JWT
- **跨域**: Flask-CORS

### 开发工具
- **版本控制**: Git
- **环境管理**: Python虚拟环境 + Node.js
- **数据库管理**: SQLite Browser

## 功能模块

### 1. 管理员功能
- 管理员登录（单账户，无注册功能）
- 管理员密码修改
- 管理员退出登录

### 2. 访客功能
- 浏览所有公开内容（无需登录）
- 查看照片、视频、日记
- 使用搜索功能
- 查看时间线

### 3. 内容管理（管理员专用）
- **照片管理**
  - 上传/删除照片
  - 照片分类(约会、日常、节日等)
  - 照片预览/轮播
  - 照片编辑(裁剪、滤镜)

- **视频管理**
  - 上传/删除视频
  - 视频分类
  - 在线播放
  - 视频缩略图生成

- **文字记录**
  - 日记/文章撰写
  - 分类管理
  - 富文本编辑器
  - 标签系统

### 4. 时间线功能
- 按时间顺序展示所有内容
- 支持筛选查看(按类型、标签、日期范围)
- 时间线样式切换

### 5. 搜索功能
- 全文搜索
- 按类型、标签、日期筛选
- 高级搜索选项

### 6. 统计功能（管理员专用）
- 内容数量统计
- 时间段内记录统计
- 数据可视化图表

## 数据库设计

### 主要表结构

#### admin (管理员表)
- id (主键)
- username (管理员用户名，默认: admin)
- password_hash (密码哈希)
- created_at (创建时间)
- updated_at (更新时间)

#### photos (照片表)
- id (主键)
- user_id (管理员ID，固定为1)
- title (标题)
- description (描述)
- filename (文件名)
- filepath (文件路径)
- category (分类)
- tags (标签)
- created_at (上传时间)
- updated_at (更新时间)

#### videos (视频表)
- id (主键)
- user_id (管理员ID，固定为1)
- title (标题)
- description (描述)
- filename (文件名)
- filepath (文件路径)
- thumbnail (缩略图)
- category (分类)
- tags (标签)
- duration (时长)
- created_at (上传时间)
- updated_at (更新时间)

#### diaries (日记表)
- id (主键)
- user_id (管理员ID，固定为1)
- title (标题)
- content (内容)
- category (分类)
- tags (标签)
- mood (心情)
- created_at (创建时间)
- updated_at (更新时间)

## API设计

### 管理员认证
- POST /api/auth/login - 管理员登录
- POST /api/auth/logout - 管理员登出
- GET /api/auth/check - 检查登录状态

### 照片管理
- GET /api/photos - 获取照片列表（所有人可访问）
- POST /api/photos - 上传照片（仅管理员）
- GET /api/photos/{id} - 获取单张照片详情（所有人可访问）
- PUT /api/photos/{id} - 更新照片信息（仅管理员）
- DELETE /api/photos/{id} - 删除照片（仅管理员）

### 视频管理
- GET /api/videos - 获取视频列表（所有人可访问）
- POST /api/videos - 上传视频（仅管理员）
- GET /api/videos/{id} - 获取单个视频详情（所有人可访问）
- PUT /api/videos/{id} - 更新视频信息（仅管理员）
- DELETE /api/videos/{id} - 删除视频（仅管理员）

### 日记管理
- GET /api/diaries - 获取日记列表（所有人可访问）
- POST /api/diaries - 创建日记（仅管理员）
- GET /api/diaries/{id} - 获取单篇日记（所有人可访问）
- PUT /api/diaries/{id} - 更新日记（仅管理员）
- DELETE /api/diaries/{id} - 删除日记（仅管理员）

### 公共功能
- GET /api/search - 搜索内容（所有人可访问）
- GET /api/timeline - 获取时间线数据（所有人可访问）
- GET /api/statistics - 获取统计数据（仅管理员）

## 项目结构

### 前端结构
```
love-diary-frontend/
├── src/
│   ├── components/     # 公共组件
│   ├── views/         # 页面组件
│   ├── router/        # 路由配置
│   ├── store/         # 状态管理
│   ├── api/           # API接口
│   ├── utils/         # 工具函数
│   └── assets/        # 静态资源
├── public/
├── package.json
└── vite.config.js
```

### 后端结构
```
love-diary-backend/
├── app/
│   ├── models/        # 数据模型
│   ├── routes/        # 路由
│   ├── utils/         # 工具函数
│   ├── config/        # 配置文件
│   └── static/        # 静态文件
├── uploads/           # 上传文件存储
├── database.db        # SQLite数据库
├── requirements.txt
└── run.py
```

## 部署方案

### 开发环境
1. 前端: `npm run dev`
2. 后端: `flask run`

### 生产环境
1. 前端: 构建静态文件部署到CDN或静态服务器
2. 后端: 使用Gunicorn + Nginx部署

## 权限控制

### 访客权限（未登录用户）
- 查看所有公开内容（照片、视频、日记）
- 使用搜索功能
- 查看时间线
- **禁止**: 内容的上传、修改、删除

### 管理员权限（登录后）
- 所有访客功能
- 内容的上传、修改、删除
- 查看统计数据
- 管理员密码修改
- 管理员退出登录

## 安全考虑

1. **文件上传安全**
   - 文件类型限制（图片: jpg, png, gif; 视频: mp4, avi, mov）
   - 文件大小限制（图片: 10MB, 视频: 100MB）
   - 文件名安全处理

2. **身份验证**
   - 管理员密码加密存储
   - Session管理
   - 登录失败次数限制

3. **数据安全**
   - SQL注入防护
   - XSS攻击防护
   - CSRF防护

4. **API安全**
   - 写操作API需要管理员权限验证
   - 错误信息不暴露敏感信息

## 扩展功能(后期可考虑)

1. 移动端适配/APP
2. 数据导入/导出
3. 备份功能
4. 多语言支持
5. 主题切换
6. 社交分享功能

## 技术难点

1. 视频上传和处理
2. 图片压缩和优化
3. 大文件分片上传
4. 前端性能优化
5. 移动端响应式设计